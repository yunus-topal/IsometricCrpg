using UnityEngine;

namespace InGameManagers
{
    /// <summary>
    /// Handle applying and removing status effects on characters.
    /// Handle status effect interactions, such as stacking, refreshing duration, and resolving conflicts between effects.
    /// </summary>
    public class StatusEffectManager : MonoBehaviour
    {
        // Start is called once before the first execution of Update after the MonoBehaviour is created
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
